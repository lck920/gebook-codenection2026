# Local Postgres for Gebook development.
# Credentials and port match DATABASE_URL in .env:
#   postgres://gebook:gebook@localhost:5430/gebook
services:
  postgres:
    image: postgres:16-alpine
    container_name: gebook-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: gebook
      POSTGRES_PASSWORD: gebook
      POSTGRES_DB: gebook
    ports:
      # Host 5430 keeps a default local Postgres on 5432 out of the way.
      - "5430:5432"
    volumes:
      - gebook-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gebook -d gebook"]
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  gebook-pgdata:
